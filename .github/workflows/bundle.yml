name: Zip UI Bundle
on:
  push:
    branches: [master]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2
      - name: Install Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '16'
      - name: Install dependencies
        run: npm install
      - name: Bundle
        run: node_modules/.bin/gulp bundle
        with:
          name: ui-bundle
          path: output/ui-bundle.zip
